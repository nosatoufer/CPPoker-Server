#ifndef GAMESTATE_H
#define GAMESTATE_H

/**
 * @brief The GameState enum represent a PokerGame state (WAITING, RUNNING or OVER)
 */
enum class GameState { WAITING, RUNNING, OVER };

#endif // GAMESTATE_H
